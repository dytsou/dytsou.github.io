# Analyze source only; dist/ is Astro build output where Layout.astro is
# inlined into every page (intentional duplication for static HTML).
sonar.sources=src,scripts
sonar.exclusions=dist/**,node_modules/**,.astro/**,**/*.generated.ts,public/**
sonar.cpd.exclusions=dist/**,**/*.generated.ts
